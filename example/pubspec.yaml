name: amplitude_flutter_new_example
description: Demonstrates how to use the amplitude_flutter_new plugin.
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  amplitude_flutter_new:
    path: ../

flutter:
  uses-material-design: true
